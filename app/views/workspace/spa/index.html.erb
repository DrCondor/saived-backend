<!DOCTYPE html>
<html lang="pl">
  <head>
    <title>SAIVED - Panel</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">

    <%# Favicon %>
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="shortcut icon" href="/favicon.ico">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "workspace", "data-turbo-track": "reload", defer: true, type: "module" %>

    <%# Pass initial data to React %>
    <%
      initial_data = {
        extensionVersion: EXTENSION_VERSION,
        currentUser: {
          id: current_user.id,
          email: current_user.email,
          first_name: current_user.first_name,
          last_name: current_user.last_name,
          full_name: current_user.full_name,
          display_name: current_user.display_name,
          initials: current_user.initials,
          company_name: current_user.company_name,
          phone: current_user.phone,
          title: current_user.title,
          avatar_url: current_user.avatar.attached? ? rails_blob_path(current_user.avatar, only_path: true) : nil,
          company_logo_url: current_user.company_logo.attached? ? rails_blob_path(current_user.company_logo, only_path: true) : nil,
          api_token: current_user.api_token,
          custom_statuses: current_user.custom_statuses,
          seen_extension_version: current_user.seen_extension_version
        }
      }
    %>
    <script>
      window.__INITIAL_DATA__ = <%= raw(initial_data.to_json) %>;
    </script>
  </head>

  <body class="min-h-screen bg-neutral-50 text-neutral-900 antialiased">
    <div id="workspace-root"></div>

    <noscript>
      <div class="flex items-center justify-center min-h-screen">
        <p class="text-neutral-600">Włącz JavaScript, aby korzystać z SAIVED.</p>
      </div>
    </noscript>
  </body>
</html>
