<!DOCTYPE html>
<html lang="pl">
  <head>
    <title>SAIVED - Panel</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">

    <%# Favicon %>
    <link rel="icon" type="image/svg+xml" href="/icon.svg">
    <link rel="icon" type="image/png" href="/icon.png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "workspace", "data-turbo-track": "reload", defer: true, type: "module" %>

    <%# Pass initial data to React %>
    <script>
      window.__INITIAL_DATA__ = {
        currentUser: {
          id: <%= current_user.id %>,
          email: "<%= j current_user.email %>",
          first_name: <%= current_user.first_name ? "\"#{j current_user.first_name}\"" : "null" %>,
          last_name: <%= current_user.last_name ? "\"#{j current_user.last_name}\"" : "null" %>,
          full_name: <%= current_user.full_name ? "\"#{j current_user.full_name}\"" : "null" %>,
          display_name: "<%= j current_user.display_name %>",
          initials: "<%= j current_user.initials %>",
          company_name: <%= current_user.company_name ? "\"#{j current_user.company_name}\"" : "null" %>,
          phone: <%= current_user.phone ? "\"#{j current_user.phone}\"" : "null" %>,
          title: <%= current_user.title ? "\"#{j current_user.title}\"" : "null" %>,
          avatar_url: <%= current_user.avatar.attached? ? "\"#{rails_blob_path(current_user.avatar, only_path: true)}\"" : "null" %>,
          api_token: "<%= j current_user.api_token %>"
        }
      };
    </script>
  </head>

  <body class="min-h-screen bg-neutral-50 text-neutral-900 antialiased">
    <div id="workspace-root"></div>

    <noscript>
      <div class="flex items-center justify-center min-h-screen">
        <p class="text-neutral-600">Włącz JavaScript, aby korzystać z SAIVED.</p>
      </div>
    </noscript>
  </body>
</html>
