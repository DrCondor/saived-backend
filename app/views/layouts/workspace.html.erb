<!DOCTYPE html>
<html>
  <head>
    <title>SAIVED – Panel</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="min-h-screen bg-neutral-50 text-neutral-900">
    <div class="min-h-screen flex flex-col">
      <!-- Header panelu -->
      <header class="border-b border-neutral-200/70 bg-white/90 backdrop-blur">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 py-3 flex items-center justify-between">
          <div class="flex items-center gap-3">
            <%= link_to root_path, class: "h-8 w-8 block" do %>
              <%= image_tag "saived-logo.jpg", class: "h-9 w-9 object-contain" %>
            <% end %>
            <div class="flex flex-col leading-tight">
              <span class="font-semibold tracking-tight text-sm sm:text-base">
                SAIVED
              </span>
              <span class="text-xs text-neutral-500">
                Panel projektów i kosztorysów
              </span>
            </div>
          </div>

          <% if current_user %>
            <div class="relative" data-controller="dropdown">
              <button
                type="button"
                data-action="click->dropdown#toggle"
                class="flex items-center gap-2 rounded-full border border-neutral-200 bg-white pl-3 pr-2 py-1.5 text-sm hover:bg-neutral-50 transition-colors"
              >
                <span class="text-neutral-600 hidden sm:inline truncate max-w-[160px]">
                  <%= current_user.email %>
                </span>
                <span class="h-7 w-7 rounded-full bg-neutral-800 text-white text-xs font-medium flex items-center justify-center">
                  <%= current_user.email[0].upcase %>
                </span>
              </button>

              <div
                data-dropdown-target="menu"
                class="hidden absolute right-0 mt-2 w-72 bg-white rounded-xl shadow-lg border border-neutral-200 py-2 z-50"
              >
                <div class="px-4 py-2 border-b border-neutral-100">
                  <p class="text-sm font-medium text-neutral-900 truncate"><%= current_user.email %></p>
                  <p class="text-xs text-neutral-500">Zalogowany</p>
                </div>

                <%= link_to edit_user_registration_path,
                    class: "flex items-center gap-3 px-4 py-2 text-sm text-neutral-700 hover:bg-neutral-50" do %>
                  <svg class="w-4 h-4 text-neutral-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                  </svg>
                  Ustawienia konta
                <% end %>

                <div class="px-4 py-2 border-t border-neutral-100 mt-1">
                  <p class="text-xs font-medium text-neutral-500 mb-1">Token API (dla rozszerzenia)</p>
                  <div class="flex items-center gap-2">
                    <code class="flex-1 text-xs bg-neutral-100 px-2 py-1 rounded font-mono truncate select-all">
                      <%= current_user.api_token %>
                    </code>
                    <button
                      type="button"
                      onclick="navigator.clipboard.writeText('<%= current_user.api_token %>'); this.textContent = 'Skopiowano!'; setTimeout(() => this.textContent = 'Kopiuj', 1500)"
                      class="text-xs text-neutral-600 hover:text-neutral-900 px-2 py-1 rounded hover:bg-neutral-100"
                    >
                      Kopiuj
                    </button>
                  </div>
                </div>

                <div class="border-t border-neutral-100 mt-1 pt-1">
                  <%= link_to destroy_user_session_path,
                      data: { turbo_method: :delete },
                      class: "flex items-center gap-3 px-4 py-2 text-sm text-red-600 hover:bg-red-50" do %>
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                    </svg>
                    Wyloguj się
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </header>

      <!-- Treść + flash -->
      <main class="flex-1">
        <% if notice.present? %>
          <div class="bg-emerald-50 text-emerald-800 text-sm py-2">
            <div class="max-w-6xl mx-auto px-4 sm:px-6">
              <%= notice %>
            </div>
          </div>
        <% end %>

        <% if alert.present? %>
          <div class="bg-rose-50 text-rose-800 text-sm py-2">
            <div class="max-w-6xl mx-auto px-4 sm:px-6">
              <%= alert %>
            </div>
          </div>
        <% end %>

        <div class="max-w-6xl mx-auto px-4 sm:px-6 py-6 sm:py-8">
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>